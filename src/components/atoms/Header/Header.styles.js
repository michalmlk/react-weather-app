import styled from "styled-components";

export const Header = styled.div`
  font-size: ${({theme}) => theme.fontSize.xl};
  width: 100%;
  padding: 1rem;
  text-align: center;
  color: #ffffff;
  font-weight: bold;
`;